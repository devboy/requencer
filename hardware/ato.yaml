requires-atopile: "^0.14.0"
paths:
  src: elec/src
  layout: elec/layout
builds:
  default:
    entry: requencer.ato:Requencer
dependencies:
  - type: registry
    identifier: generics
    release: "0.2.0"
