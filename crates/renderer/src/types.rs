//! UI types — re-exported from engine crate.
//!
//! The canonical definitions live in `requencer_engine::ui_types`.
//! This module re-exports them so existing renderer code continues to compile.

pub use requencer_engine::ui_types::*;
