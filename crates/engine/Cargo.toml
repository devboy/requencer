[package]
name = "requencer-engine"
version = "0.1.0"
edition = "2021"

[features]
default = []
std = []

[dependencies]
heapless = { workspace = true }
libm = "0.2"
serde = { workspace = true }
postcard = { workspace = true }

[dev-dependencies]
